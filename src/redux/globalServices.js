/** global services middleware */
import { carsMiddleware } from './feature/carsDetails/service';

/**
 * Add Another service in here.
 */
const globalService = [carsMiddleware];

export default globalService;
